package com.arena.app.iam.application.service;
import com.arena.app.iam.domain.repository.*;
import com.arena.app.scheduling.domain.repository.*;
import com.arena.app.iam.application.service.*;
import com.arena.app.scheduling.application.service.*;
import com.arena.app.iam.domain.model.*;
import com.arena.app.scheduling.domain.model.*;

//import com.arena.app.iam.application.service.EmailService;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import java.util.Scanner;

@SpringBootTest
public class EmailServiceTest {
/*     @Autowired
    private EmailService emailService;
    @Test
    public void sendEmailTest() {
        var scanner = new Scanner(System.in);
        System.out.println("Insira o seu endereço de email: ");
        var email = scanner.nextLine();
        emailService.sendEmail(email,"Testando envio de email de recuperação", "Olá, este é um email de teste para verificar o funcionamento do serviço de envio de emails. Se você recebeu este email, significa que o serviço está funcionando corretamente.");
    } */
}
