name: Docker CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build with Docker Compose
        run: docker compose build

      - name: Verify Container Build
        run: |
          docker compose up -d
          # Aguarda um pouco para o Spring Boot iniciar
          sleep 30
          # Verifica se o container está rodando
          docker compose ps
          # Testa a resposta da aplicação
          curl --fail http://localhost:8080 || exit 1
          docker compose down
<<<<<<< HEAD

      - name: Create PR to prod
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create --base prod --head main --title "Deploy: main to prod" --body "PR automático criado após sucesso no Docker CI." || echo "PR já existe ou não há mudanças para mesclar."

=======
>>>>>>> 5742b5574f5f7122cbd6bd53bceb07aba9b8f769
